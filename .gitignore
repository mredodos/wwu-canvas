# Internal working files — intentionally untracked in the public repo.
# They live on the maintainer's machine only.
.claude/
CLAUDE.md
MASTER-*.md
docs/
tests/

# OS / editor noise
.DS_Store
Thumbs.db
node_modules/

# AI agent / editor config — personal, never versioned.
# NOTE: `.cursor/` does NOT cover `.cursorrules` — Cursor keeps rules in two
# places (modern `.cursor/rules/` directory + legacy root file). Both needed.
.cursor/
.cursorrules
